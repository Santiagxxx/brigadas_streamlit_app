# Prueba local con PostgreSQL

1. Configura `.streamlit/secrets.toml`.
2. Instala dependencias:

```bash
pip install -r requirements.txt
```

3. Ejecuta:

```bash
streamlit run app.py
```

4. Ingresa un registro y valida la descarga desde Admin.
