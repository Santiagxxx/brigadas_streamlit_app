# Cadena de conexión PostgreSQL

Formato recomendado:

```text
postgresql://usuario:password@host:5432/base?sslmode=require
```

En Streamlit Community Cloud esta cadena debe ir en Secrets como `DATABASE_URL`.
